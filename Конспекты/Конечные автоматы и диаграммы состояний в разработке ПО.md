---
дисциплина: "05.01"
курс: 2
tags:
  - тип/теория
---
### Введение: от математики к практике

Исторический контекст

· 1950-е: Конечные автоматы (FSM) как математическая модель в теории вычислений
· 1980-е: Дэвид Харрел представляет Statecharts — расширение FSM для сложных систем
· 1990-е: UML стандартизирует диаграммы состояний
· 2020-е: Statecharts активно используются в веб, мобильной разработке, IoT

### 📊 Часть 1: Теоретическая основа — Конечные автомат

#### 1.1 Формальное определение FSM

Конечный автомат — это кортеж из 5 элементов:

M = (Q, Σ, δ, q₀, F)
Где:

· Q — конечное множество состояний
· Σ — конечный входной алфавит
· δ — функция переходов: Q × Σ → Q
· q₀ ∈ Q — начальное состояние
· F ⊆ Q — множество конечных (принимающих) состояний

#### 1.2 Типы автоматов

Автомат Мили (Mealy Machine)

Выход зависит от состояния И входа
δ: Q × Σ → Q
λ: Q × Σ → Γ (функция выходов)

```mermaid
graph LR
    A[S0] -->|a/x| B[S1]
    B -->|b/y| C[S2]
```

Автомат Мура (Moore Machine)

Выход зависит только от состояния
λ: Q → Γ (выход привязан к состоянию)

```mermaid
graph LR
    A[S0/output1] -->|a| B[S1/output2]
    B -->|b| C[S2/output3]
```

#### 1.3 Простой пример FSM: Дверь

Состояния: {Закрыта, Открыта, Блокирована}
События: {открыть, закрыть, заблокировать, разблокировать}

```python
# Псевдокод FSM для двери
class DoorFSM:
    states = {'closed', 'open', 'locked'}
    current_state = 'closed'
    
    transitions = {
        'closed': {
            'open': 'open',
            'lock': 'locked'
        },
        'open': {
            'close': 'closed'
        },
        'locked': {
            'unlock': 'closed'
        }
    }
    
    def send(self, event):
        if event in self.transitions[self.current_state]:
            self.current_state = self.transitions[self.current_state][event]
```

### 🎨 Часть 2: Практическая реализация — Диаграммы состояний UML

#### 2.1 Базовые элементы нотации

```plantuml
@startuml
title Пример базовой диаграммы состояний

state "IDLE" as idle
state "PROCESSING" as proc
state "COMPLETED" as comp

[*] --> idle
idle --> proc : startProcessing [isValid]\n/ initialize()
proc --> comp : complete / notifyUser()
proc --> idle : cancel / cleanup()
comp --> [*]

@enduml
```

#### 2.2 Полное обозначение состояния

┌───────────────────────┐
│    НазваниеСостояния  │
├───────────────────────┤
│ entry / действиеВхода │
│ do    / активность    │
│ exit  / действиеВыхода│
└───────────────────────┘

```plantuml
@startuml
state "Загрузка данных" as loading {
  state "Получение данных" as fetching
  state "Парсинг данных" as parsing

  [*] --> fetching : Начало загрузки
  fetching --> parsing : dataReceived / parseData()
  parsing --> [*] : parsingComplete

  loading : entry / showSpinner()
  loading : exit / hideSpinner()
  loading : do / monitorProgress()
}
@enduml
```

#### 2.3 Специальные состояния и псевдо состояния

```plantuml
@startuml
' Начальное состояние
state "Initial" as init <<start>>

' Конечное состояние  
state "Final" as fin <<end>>

' Историческое состояние
state "History" as hist <<history>>

' Точка соединения
state "Junction" as junc <<choice>>

' Точка слияния/ветвления
state "Fork" as fork <<fork>>
state "Join" as join <<join>>

init --> [*] : в основной граф
[*] --> fin : завершение
@enduml
```

### 🔄 Часть 3: Переходы и события

#### 3.1 Полный формат перехода

┌───────────────────────────────────────────────────────┐
│   событие(параметры) [условие] / действие            │
│   ^            ^         ^          ^                 │
│   триггер      аргументы охрана     эффект           │
└───────────────────────────────────────────────────────┘

#### 3.2 Примеры переходов

```plantuml
@startuml
state Form {
  state Empty
  state Filling
  state Validating
  state Submitting
  
  [*] --> Empty
  Empty --> Filling : onInput() / updateField()
  Filling --> Validating : onSubmit() [fieldsFilled]\n/ validate()
  
  Validating --> Submitting : onValidationSuccess()\n[isValid == true]\n/ submitToAPI()
  
  Validating --> Filling : onValidationFail()\n[hasErrors]\n/ showErrors()
  
  Submitting --> [*] : onSuccess() / showSuccess()
  Submitting --> Filling : onError() / showError()
}

@enduml
```

### 🏗️ Часть 4: Составные и параллельные состояния

#### 4.1 Иерархия состояний

```plantuml
@startuml
state "Система заказов" as order_system {
  state "Оформление" as checkout {
    state "Корзина" as cart
    state "Доставка" as shipping
    state "Оплата" as payment
    
    [*] --> cart
    cart --> shipping : next() [cartNotEmpty]
    shipping --> payment : next() [addressValid]
    payment --> [*] : complete()
  }
  
  state "Исполнение" as fulfillment {
    state "Подтверждён" as confirmed
    state "Собран" as packed
    state "Отправлен" as shipped
    state "Доставлен" as delivered
    
    [*] --> confirmed
    confirmed --> packed : pack()
    packed --> shipped : ship()
    shipped --> delivered : deliver()
  }
  
  state "Возвраты" as returns <<fork>>
  
  checkout --> fulfillment : orderConfirmed
  fulfillment --> returns : returnRequested
}
@enduml
```

#### 4.2 Параллельные (ортогональные) состояния

```plantuml
@startuml
state "Пользовательская сессия" as session {
  state "Аутентификация" as auth {
    state "Гость" as guest
    state "Аутентифицирован" as authenticated
    state "Админ" as admin
    
    [*] --> guest
    guest --> authenticated : login() [credentialsValid]
    authenticated --> admin : elevate() [isAdmin]
  }
  
  state "Соединение" as connection {
    state "Офлайн" as offline
    state "Онлайн" as online
    state "Переподключение" as reconnecting
    
    [*] --> offline
    offline --> online : connect()
    online --> reconnecting : connectionLost()
    reconnecting --> online : reconnected()
  }
  
  -- 
  guest --> offline : начальное
  authenticated --> online : после входа
}
@enduml
```

#### 4.3 Историческое состояние

```plantuml
@startuml
state "Редактор документа" as editor {
  state "Просмотр" as view <<history>>
  state "Редактирование" as edit
  
  state view {
    state "Оглавление" as toc
    state "Содержание" as content
    state "Комментарии" as comments
    
    [*] --> toc
    toc --> content : selectSection()
    content --> comments : showComments()
    comments --> content : hideComments()
  }
  
  state edit {
    state "Текст" as text_edit
    state "Изображения" as image_edit
    
    [*] --> text_edit
    text_edit --> image_edit : editImage()
  }
  
  [*] --> view
  view --> edit : editDocument()
  edit --> view : H / saveDocument()
}
@enduml
```

### 💻 Часть 5: Практические примеры из разработки

#### 5.1 Веб-форма с валидацией

```plantuml
@startuml
title Состояния формы регистрации

state RegistrationForm {
  state "EMPTY" as empty
  state "FILLING" as filling
  state "VALIDATING" as validating
  state "SUBMITTING" as submitting
  state "SUCCESS" as success
  state "ERROR" as error {
    state "CLIENT_ERROR" as client_err
    state "SERVER_ERROR" as server_err
    state "NETWORK_ERROR" as network_err
  }
  
  [*] --> empty
  
  empty --> filling : onInput()\n/ initForm()
  
  filling --> validating : onSubmit() [allFieldsFilled]\n/ validateClientSide()
  
  validating --> filling : onValidationFail()\n/hasErrors/\n showErrors()
  
  validating --> submitting : onValidationSuccess()\n[isValid]\n/ submitData()
  
  submitting --> success : onSuccess(201)\n/ showSuccessMessage()
  
  submitting --> client_err : onError(400)\n/ showFieldErrors()
  submitting --> server_err : onError(500)\n/ showServerError()
  submitting --> network_err : onTimeout()\n/ showNetworkError()
  
  client_err --> filling : retry()\n/ clearErrors()
  server_err --> submitting : retry()\n/ resubmit()
  network_err --> submitting : retry()\n/ resubmit()
  
  success --> [*] : continue()\n/ redirect()
  
  note right of filling
    Поля формы:
    - email
    - password
    - confirmPassword
    - согласие с условиями
  end note
  
  note left of validating
    Валидация включает:
    1. Формат email
    2. Силу пароля
    3. Совпадение паролей
    4. Чекбокс обязателен
  end note
}

@enduml
```

#### 5.2 API клиент с retry логикой

```plantuml
@startuml
title Statechart для HTTP‑клиента с повторными попытками

state APIClient {
  state "IDLE" as idle
  state "FETCHING" as fetching
  state "RETRY_WAIT" as retry_wait
  state "SUCCESS" as success
  state "FAILURE" as failure

  [*] --> idle

  idle --> fetching : fetch(url, options)\n/ startRequest()

  state fetching {
    entry : startTimeout()
    exit : clearTimeout()

    state "REQUEST_SENT" as sent
    state "RESPONSE_PENDING" as pending

    [*] --> sent : sendRequest()
    sent --> pending : requestSent
    pending --> [*] : responseReceived
  }

  fetching --> success : onResponse(2xx)\n/ processData()

  fetching --> retry_wait : onResponse(5xx)\n[retryCount < MAX_RETRIES]\n/ incrementRetry()

  retry_wait --> fetching : retryTimeout\n/ retryRequest()

  fetching --> failure : onResponse(4xx)\n/ handleClientError()

  retry_wait --> failure : [retryCount >= MAX_RETRIES]\n/ handleMaxRetries()

  success --> idle : reset()
  failure --> idle : reset()

  note right of retry_wait
    Экспоненциальная backoff:\n
    Задержка = $2^{\text{retryCount}} \times \text{BASE\_DELAY}$\n
    MAX_RETRIES = 3\n
    BASE_DELAY = 1000 мс
  end note
}
@enduml
```

### 🛠️ Часть 6: Реализация в коде

#### 6.1 Простая реализация FSM на JavaScript

```JS
class FiniteStateMachine {
  constructor(states, initialState) {
    this.states = states;
    this.currentState = initialState;
    this.listeners = new Set();
  }
  
  transition(event, data) {
    const currentStateConfig = this.states[this.currentState];
    
    if (!currentStateConfig.transitions[event]) {
      console.warn(`No transition for event "${event}" in state "${this.currentState}"`);
      return;
    }
    
    const transition = currentStateConfig.transitions[event];
    
    // Проверка условия (guard)
    if (transition.guard && !transition.guard(data)) {
      return;
    }
    
    // Выходное действие текущего состояния
    if (currentStateConfig.onExit) {
      currentStateConfig.onExit();
    }
    
    // Действие перехода
    if (transition.action) {
      transition.action(data);
    }
    
    const previousState = this.currentState;
    this.currentState = transition.target;
    
    // Входное действие нового состояния
    const newStateConfig = this.states[this.currentState];
    if (newStateConfig.onEnter) {
      newStateConfig.onEnter();
    }
    
    // Уведомление слушателей
    this.notify(previousState, this.currentState, event, data);
  }
  
  // Пример использования
  static createDoorFSM() {
    return new FiniteStateMachine({
      closed: {
        onEnter: () => console.log('Дверь закрыта'),
        transitions: {
          open: { target: 'open', action: () => console.log('Открываю дверь') },
          lock: { target: 'locked', action: () => console.log('Блокирую дверь') }
        }
      },
      open: {
        onEnter: () => console.log('Дверь открыта'),
        transitions: {
          close: { target: 'closed', action: () => console.log('Закрываю дверь') }
        }
      },
      locked: {
        onEnter: () => console.log('Дверь заблокирована'),
        transitions: {
          unlock: { 
            target: 'closed', 
            action: () => console.log('Разблокирую дверь'),
            guard: (data) => data.hasKey // Условие: есть ключ
          }
        }
      }
    }, 'closed');
  }
}

// Использование
const door = FiniteStateMachine.createDoorFSM();
door.transition('open'); // Открывает дверь
door.transition('close'); // Закрывает дверь
door.transition('lock'); // Блокирует
door.transition('unlock', { hasKey: true }); // Разблокирует с ключом
```

#### 6.2 Современный подход с XState

```JS
import { createMachine, interpret } from 'xstate';

// Statechart для формы входа
const loginMachine = createMachine({
  id: 'login',
  initial: 'idle',
  context: {
    username: '',
    password: '',
    error: null,
    retries: 0
  },
  states: {
    idle: {
      on: {
        INPUT_CHANGE: {
          actions: 'updateField'
        },
        SUBMIT: {
          target: 'validating',
          cond: 'formFilled'
        }
      }
    },
    validating: {
      entry: 'validateForm',
      on: {
        VALIDATION_SUCCESS: 'submitting',
        VALIDATION_FAILED: {
          target: 'idle',
          actions: 'setErrors'
        }
      }
    },
    submitting: {
      entry: 'showLoading',
      invoke: {
        src: 'submitToAPI',
        onDone: {
          target: 'success',
          actions: 'onLoginSuccess'
        },
        onError: {
          target: 'error',
          actions: 'onLoginError'
        }
      },
      on: {
        CANCEL: 'idle'
      }
    },
    success: {
      type: 'final',
      entry: 'redirectToDashboard'
    },
    error: {
      on: {
        RETRY: {
          target: 'submitting',
          cond: 'canRetry',
          actions: 'incrementRetries'
        },
        RESET: 'idle'
      }
    }
  }
}, {
  actions: {
    updateField: (context, event) => {
      context[event.field] = event.value;
    }
  },
  guards: {

formFilled: (context) => 
      context.username && context.password,
    canRetry: (context) => 
      context.retries < 3
  },
  services: {
    submitToAPI: async (context) => {
      const response = await fetch('/api/login', {
        method: 'POST',
        body: JSON.stringify({
          username: context.username,
          password: context.password
        })
      });
      if (!response.ok) throw new Error('Login failed');
      return response.json();
    }
  }
});

// Запуск машины
const loginService = interpret(loginMachine)
  .onTransition((state) => {
    console.log('Текущее состояние:', state.value);
  })
  .start();

// Отправка событий
loginService.send({
  type: 'INPUT_CHANGE',
  field: 'username',
  value: 'user@example.com'
});
```

# 📈 Часть 7: Паттерны и анти паттерны

#### 7.1 Хорошие практики

```plantuml
@startuml
title Хорошая структура statechart

state GoodDesign {
  state "Чёткие названия" as clear_names
  state "Иерархия" as hierarchy
  state "Обработка ошибок" as error_handling
  state "Восстанавливаемость" as recoverable
  
  clear_names : Состояния называются\nкак ситуации, а не действия\n"idle", а не "waitingForInput"
  
  hierarchy : Сложные состояния\nразбиваются на подсостояния
  
  error_handling : Все возможные ошибки\nявно представлены как состояния
  
  recoverable : Из любого состояния\nесть путь к нормальной работе
}

@enduml
```

#### 7.2 Анти паттерны

```plantuml
@startuml
title Типичные ошибки в statecharts

state AntiPatterns {
  state "Состояние-мусорка" as god_state
  state "Переходы-спагетти" as spaghetti
  state "Неявные состояния" as implicit
  state "Состояние-действие" as action_state
  
  god_state : Одно состояние делает слишком много\nи имеет десятки переходов
  
  spaghetti : Все связаны со всеми\nнет чёткой структуры
  
  implicit : Состояния не указаны явно\nно подразумеваются через флаги
  
  action_state : Состояние названо как действие\n"validating" вместо "validation"
}
@enduml
```

### 🎓 Часть 8: Применение в реальных проектах

#### 8.1 Где использовать state charts:

1. UI компоненты — формы, модальные окна, виджеты
2. Процессы — оформление заказа, регистрация, onboarding
3. Анимации — состояния анимаций, переходы
4. Игры — AI, физика, управление персонажем
5. IoT устройства — протоколы связи, управление питанием
6. Бизнес-процессы — workflow, approval flows

#### 8.2 Инструменты и библиотеки:

· Визуализация: PlantUML, Mermaid.js, draw.io
· Библиотеки: XState (JS), Robot (JS), Stateless (.NET), SwiftState (iOS)
· Сервисы: Stately.ai (визуальный редактор + кодогенерация)
· Стандарты: SCXML (W3C стандарт для statecharts)

### 📚 Заключение и ключевые выводы

Связь теория ↔ практика:

Теория (FSM) → Расширение (Statecharts) → Практика (UML) → Код (библиотеки)
`

Что важно запомнить:

1. FSM — математическая основа, Statecharts — практическое расширение
2. Иерархия и параллелизм — ключевые преимущества statecharts
3. Диаграммы UML — стандарт визуализации для команды
4. Современные библиотеки делают statecharts исполняемыми
5. Паттерны помогают избежать типичных ошибок

Философия:

"Сложное поведение = Простые состояния + Чёткие переходы"

Statecharts помогают управлять сложностью через:

· Декомпозицию (разбиение на подсостояния)
· Детерминизм (предсказуемое поведение)
· Визуализацию (понятно всей команде)
· Тестируемость (легко покрыть все переходы)

📖 Дополнительные ресурсы

1. Книги:
   · "Practical UML Statecharts in C/C++" by Miro Samek
   · "Programming Game AI by Example" by Mat Buckland
2. Статьи:
   · Statecharts.github.io — онлайн курс
   · CSS-Tricks: "State Machines in JavaScript"
3. Видео:
   · "The World of Statecharts" — David Khourshid
   · "Infinitely Better UIs with Finite Automata" — Kyle Shevlin